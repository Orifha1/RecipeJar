
<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/recipes">RecipeJar</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <!-- <a class="nav-link" href="/">Home</a> -->
          <a class="nav-link" href="/recipes">Recipes</a>
          <a class="nav-link" href="/recipes/new">New Recipe</a>
        </div>
        <div class="navbar-nav ms-auto">
          <% if(!currentUser) {%>
          <a class="nav-link" href="/login">Login</a>
          <a class="nav-link" href="/register">Register</a>
          <% } else {%>
            <div class="dropdown list-group">
              <a href="#" class="dropdown-toggle nav-link text-secondary" data-bs-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Notifications <span class="badge badge-primary badge-pill"><%= notifications.length %></span></a>
              <ul class="dropdown-menu bg-light" style="padding: 0px;">
                <li class="list-group-item">
                    <a href="/notifications" class="notificationLinks">Past notifications</a>
                </li>
                <% notifications.forEach(function(notification) { %>                          
                    <li class="list-group-item">
                        <a href="/notifications/<%= notification.id %>" class="notificationLinks">
                            <%= notification.username %> created a new campground
                        </a>
                    </li>
                <% }); %>
              </ul>
            </div>
            
          <a class="nav-link" href="/users/<%= currentUser._id %>">Signed In As <%= currentUser.username %></a>
          <a class="nav-link" href="/logout">Logout</a>
          <% } %>
        </div>
        
      </div>
    </div>
  </nav>